require_relative "entity.rb"
module MuseumProvenance
  # {Location} is a data structure for holding the location of a {Period}.  
  # Currently, it does not have any functionality beyond that of a generic {Entity}
  # but it is intended be extended to include geographical functionality.
  class Location < Entity
  end
end   